- name: Loops topic
  connection: local
  hosts: local
  vars: 
  instance: 
  - a 
  - b
  - c
  tasks:
  - name: print variables in loop
    Loop: "{{ instance }}"
    register: output
    ansible.builtin.debug:
      msg: "{{ output }}"
    